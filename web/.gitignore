node_modules
# Keep environment variables out of version control
.env

# SQLite (local auth DB)
prisma/*.db
prisma/*.db-journal

# Agency branding uploads (keep directory via .gitkeep)
public/uploads/agency/**/*
!public/uploads/agency/.gitkeep
