[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.agent-api"

[deploy]
startCommand = "python -m agent_api"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
